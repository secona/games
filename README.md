# GAMES

A collection of quick browser games built with React, TypeScript, Vite, and
TanStack Router. The production build is an installable progressive web app
with offline navigation and automatic service-worker updates.

## Development

```bash
pnpm install
pnpm dev
pnpm lint
pnpm build
pnpm preview
```
